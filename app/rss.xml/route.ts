import { articles,profile } from '@/lib/content';
export const dynamic = 'force-static';
export function GET(){const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const items=articles.slice().reverse().map(a=>`<item><title><![CDATA[${a.title}]]></title><link>${base}/writing/${a.slug}</link><guid>${base}/writing/${a.slug}</guid><pubDate>${new Date(a.date).toUTCString()}</pubDate><description><![CDATA[${a.description}]]></description></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${profile.name}</title><link>${base}</link><description>${profile.headline}</description>${items}</channel></rss>`;return new Response(xml,{headers:{'content-type':'application/rss+xml; charset=utf-8','cache-control':'public, max-age=3600'}})}

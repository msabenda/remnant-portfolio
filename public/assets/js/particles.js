// Empty file since particle logic is now in Hero.vue
// Kept for potential future enhancements
document.addEventListener('DOMContentLoaded', () => {
  // No additional logic needed; particles managed in Hero.vue
});
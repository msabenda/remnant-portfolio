const nodes=['Developer','Gateway','Identity','Policy','AI agent','Tools','Community'];
export function ApiTopology(){return <div className="topology" aria-label="Request path through API and AI trust boundaries"><div className="topology-head"><span>REQUEST TOPOLOGY</span><span className="status"><i/> CONTROLLED</span></div><div className="topology-map">{nodes.map((node,i)=><div className="topology-node" key={node}><span>{String(i+1).padStart(2,'0')}</span><strong>{node}</strong>{i<nodes.length-1&&<b aria-hidden="true"/>}</div>)}</div><div className="topology-foot"><code>POST /future</code><span>identity → policy → capability</span></div></div>}

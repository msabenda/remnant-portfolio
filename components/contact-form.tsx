'use client';
import { useState } from 'react';
export function ContactForm(){const[state,setState]=useState('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');try{const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setState(r.ok?'sent':'error')}catch{setState('error')}}return <form className="contact-form" onSubmit={submit}><div className="field-row"><label>Name<input name="name" required minLength={2} maxLength={80}/></label><label>Email<input name="email" type="email" required maxLength={160}/></label></div><label>Organization <span>optional</span><input name="organization" maxLength={120}/></label><label>What brings you here?<select name="intent" defaultValue="security"><option value="security">API security engagement</option><option value="research">Agentic AI research</option><option value="devrel">DevRel collaboration</option><option value="speaking">Speaking or workshop</option><option value="apicon">APICon Tanzania partnership</option><option value="general">General inquiry</option></select></label><label>Context<textarea name="message" required minLength={20} maxLength={3000} rows={6} placeholder="Share the problem, audience, or outcome you have in mind."/></label><input className="honeypot" name="website" aria-label="Leave this field empty" tabIndex={-1} autoComplete="off"/><button className="button primary" disabled={state==='sending'}>{state==='sending'?'Sending…':'Send inquiry →'}</button><p role="status" className="form-status">{state==='sent'?'Message received. I’ll reply soon.':state==='error'?'Message could not be sent. Please email me directly.':''}</p></form>}

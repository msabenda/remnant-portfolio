# Google Sheets / Apps Script — Web App URL (ends with /exec)
# Deploy script as Web app, paste URL here. Rebuild before deploy: npm run build
# If rows still do not appear: open scripts/google-apps-script/contact-webapp.gs — use a
# sheet-bound script OR set Script property SPREADSHEET_ID in Apps Script project settings.
VITE_GOOGLE_APPS_SCRIPT_URL=

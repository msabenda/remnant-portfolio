import { PageShell } from '@/components/page-shell'; import { expertise } from '@/lib/content';
export const metadata={title:'Research'}; export default function Page(){return <PageShell index="02 · RESEARCH" title="Hostile questions. Useful answers." intro="Research into API abuse, identity boundaries, and the security model of autonomous agents."><div className="expertise-map">{expertise.slice(0,3).map((x,i)=><article className="expertise-node" key={x.code}><div><span>R-{i+1}</span><code>{x.code}</code></div><h2>{x.title}</h2><p>{x.text}</p></article>)}</div><div className="notice">Publication-ready research artifacts and responsible-disclosure details are still required. No findings are fabricated here.</div></PageShell>}

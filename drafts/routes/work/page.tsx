import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import { PageShell } from '@/components/page-shell'; import { works } from '@/lib/content';
export const metadata={title:'Work'}; export default function Page(){return <PageShell index="01 · WORK" title="Systems, not surfaces." intro="Selected security, research, education, and community work—structured around decisions and evidence."><div className="work-list">{works.map(w=><Link className="work-row" href={w.slug==='apicon-tanzania'?'/apicon-tanzania':`/work/${w.slug}`} key={w.slug}><span className="work-index">{w.index}</span><div><small>{w.type}</small><h2>{w.title}</h2><p>{w.description}</p><div className="tags">{w.tags.map(t=><span key={t}>{t}</span>)}</div></div><ArrowUpRight/></Link>)}</div></PageShell>}

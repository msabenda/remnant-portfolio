import { notFound } from 'next/navigation'; import Link from 'next/link'; import { works } from '@/lib/content';
export function generateStaticParams(){return works.filter(w=>w.slug!=='apicon-tanzania').map(w=>({slug:w.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const w=works.find(x=>x.slug===slug);if(!w)notFound();return <><header className="page-hero shell"><p className="eyebrow">{w.type}</p><h1>{w.title}</h1><p>{w.description}</p></header><section className="section shell content-grid"><aside className="meta-rail"><div>{w.status}</div>{w.tags.map(t=><div key={t}>{t}</div>)}</aside><article className="prose"><h2>Evidence before embellishment.</h2><p>This case-study structure is ready for context, threat model, personal contribution, constraints, decisions, outcomes, and lessons.</p><div className="notice">The legacy portfolio does not contain enough public evidence to complete this case study honestly. Supply a disclosure-safe project brief and supporting links.</div><h3>Planned structure</h3><ul><li>Context and threat model</li><li>Scope, role, and constraints</li><li>Approach and technical decisions</li><li>Outcome and lessons</li><li>Standards, tools, and evidence</li></ul><Link className="button" href="/contact">Discuss this practice →</Link></article></section></>}

import { PageShell } from '@/components/page-shell'; export const metadata={title:'Uses'}; export default function Page(){return <PageShell index="07 · USES" title="Tools follow the threat model." intro="A future inventory of the tools, workflows, and references behind the practice."><div className="notice">Tooling will be added only after the current laboratory setup and recommendations are verified.</div></PageShell>}
